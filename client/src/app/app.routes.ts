import { Routes } from '@angular/router';
import { AdvertisementList } from './components/advertisement-list/advertisement-list';

export const routes: Routes = [
    { path: '', component: AdvertisementList }
];
